{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window streams snapshots from the backend, so it needs the core event permissions. Nothing else is granted: no filesystem, shell or network access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
