[package]
name = "btm-model"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Turns raw process samples into apps a person can recognise."

[dependencies]
btm-probe = { path = "../btm-probe" }
serde.workspace = true

[[bin]]
name = "btm-apps"
path = "src/bin/apps.rs"
